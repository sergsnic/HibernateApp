create table person (
    id int primary key,
    name varchar,
    age int
);

insert into person values (1,'Tet person',30);

select * from person;

drop table person;

create table person(
    id   int primary key generated by default as identity,
    name varchar,
    age  int
);

update person
set name = 'Pen', age = 45
where id = 7